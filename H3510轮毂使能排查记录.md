# H3510 轮毂使能排查记录

## 目标

只完成左右 H3510 轮毂使能确认，不向 DM4310 关节和舵机发送运动命令。

## 已尝试的软件路径

1. 按 H3510 手册，对左右轮毂目标 ID `2`、`5` 发送使能帧：
   - MIT 模式 ID：`0x002`、`0x005`
   - 位置速度模式 ID：`0x102`、`0x105`
   - 速度模式 ID：`0x202`、`0x205`
   - 力位混控模式 ID：`0x302`、`0x305`
   - 数据均为 `FF FF FF FF FF FF FF FC`

2. 按 H3510 手册发送参数读取帧：
   - 报文 ID：`0x7FF`
   - 数据：`ESC_ID_L ESC_ID_H 0x33 RID ...`
   - 扫描 `ESC_ID=0..8`
   - 读取寄存器：
     - `0x07`：`MST_ID`
     - `0x08`：`ESC_ID`
     - `0x0A`：`CTRL_MODE`
     - `0x23`：`can_br`

3. 参数读取帧分别尝试过：
   - DLC = 4：未收到回复
   - DLC = 8：收到回复

4. 通过 GDB 读取 CAN2 状态：
   - CAN2 发送状态正常
   - `ESR = 0`
   - 发送邮箱正常释放
   - 说明 MCU CAN2 本身没有报总线错误

## 实测结果

### 参数读取扫描

DLC=8 时，只有以下 ESC_ID 回复：

| ESC_ID | MST_ID | ESC_ID寄存器 | CTRL_MODE | can_br |
|---:|---:|---:|---:|---:|
| 1 | 0 | 1 | 1(MIT) | 4(1Mbps) |
| 4 | 0 | 4 | 1(MIT) | 4(1Mbps) |

`ESC_ID=2` 和 `ESC_ID=5` 没有任何参数回复。

### 在线节点类型判断

对 ID1 读取只读特征寄存器：

| 寄存器 | 返回 |
|---|---:|
| `0x10 NPP` | `14` |
| `0x14 Gr` | `10.0` |

而 H3510 手册参数是：

| 参数 | H3510 手册值 |
|---|---:|
| 极对数 NPP | `7` |
| 减速比 Gr | `1:1` |

因此当前 CAN2 上被读到的 ID1/ID4 更像 DM4310 关节电机，不是 H3510 轮毂。

## 当前结论

从软件角度，控制板 CAN2 已经可以：

- 正常发送标准 CAN 帧
- 正常接收达妙参数回复
- 正常读取 ID1/ID4 的达妙设备参数

但 H3510 轮毂没有以 `ESC_ID=2` 或 `ESC_ID=5` 出现在 CAN2 上，也没有在 `ESC_ID=0..8` 扫描中出现。

所以目前不能再把问题归因为“使能帧格式不对”或“当前代码没有尝试正确模式 ID”。更大的嫌疑在代码外部。

## 仍怀疑的代码外问题

1. H3510 没有真正接到当前 CAN2 总线。
2. H3510 8pin 端子线序或视角镜像接错。
   - 手册线序：`VCC VCC VCC GND GND GND CAN_L CAN_H`
3. H3510 没有 24V 电机供电，或电机地没有和控制板 CAN 地可靠共地。
4. H3510 的实际 `ESC_ID` 不在 `0..8` 范围内。
5. H3510 的 CAN 波特率/反馈模式被改成异常配置，例如 CANFD 相关配置。
6. 当前实际接在线上的两个达妙节点可能仍是 DM4310，而不是 H3510。

## 建议下一步硬件确认

1. 只接控制板和单个 H3510，完全断开 DM4310 关节电机。
2. 断电确认 CANH-CANL 电阻约 `120Ω` 或接入两端终端后约 `60Ω`。
3. 上电确认 H3510 24V 到位，并确认控制板 GND 与 H3510 GND 共地。
4. 按手册 8pin 图用万用表逐脚确认：
   - 前 3 脚为 VCC
   - 中间 3 脚为 GND
   - 最后 2 脚为 CAN_L、CAN_H
5. 使用达妙调试助手或串口启动信息确认 H3510 当前：
   - `ESC_ID`
   - `MST_ID`
   - `CTRL_MODE`
   - `can_br`

