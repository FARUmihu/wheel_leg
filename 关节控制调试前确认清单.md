# 关节控制调试前确认清单

  ## 1. 本阶段目标

  当前阶段是否按这个目标执行？
  - 顺序：左腿 -> 右腿
  - 单腿内顺序：DM4310 关节 -> 飞特舵机
  - 目标优先级：先位置准确，允许小误差；后续再优化响应速度、超调、平滑性
  - 暂不做完整安全保护，只做调试所需的最小限幅/手动停机
答：是的。

  请确认“允许小误差”的量化标准：

  - DM4310 位置误差允许多少？
    - `±0.05 rad` 
  - 舵机角度误差允许多少？
    - `±3 deg`
  - 足端 D 点误差是否现在就考核？
    - 现在就考核， `±5 mm` 

  ## 2. 硬件连接

  请确认当前硬件连接：

  - DM4310 左腿关节 CAN ID 是不是 `1`？——是的。
  - DM4310 右腿关节 CAN ID 是不是 `4`？——是的。
  - 飞特左腿舵机 ID 是不是 `0`？——是的。
  - 飞特右腿舵机 ID 是不是 `3`？——是的。
  - 飞特使用的是 `USART2`，波特率 `1 Mbps`，是否正确？—— 正确
  - CAN2 是否只接 DM 电机，CAN1 是否只接 IMU？——目前还没接上 IMU，但后续要接上。

  ## 3. DM4310 零点和方向

  左腿 DM4310 先调。请确认：

  - DM4310 当前是否已经设过机械零点？——是的。
  - `g_dm_motors[DM_MOTOR_LEFT_JOINT_IDX].pos` 增大时，左腿关节实际往哪个方向转？——逆时针
  - 当前代码认为左腿 DM 映射是：
    - `dm_rad = 2.22 -> theta2 = -170 deg`
    - `dm_rad = 0.00 -> theta2 = -45 deg`
  - 这个方向是否和实物一致？——是的。

  建议你准备 3 个左腿 DM 测试位置：

  ```text
  dm_rad = 0.30
  dm_rad = 1.10
  dm_rad = 1.90

  你需要告诉我每个位置下，机构实际是往预期方向动，还是方向反了。——你自己去看吧，相关的映射都写在代码里了。
  ``

  ## 4. 舵机标定

  当前代码认为：

  左腿 id0:
  raw 32   -> theta1  25 deg
  raw 1005 -> theta1 -180 deg

  右腿 id3:
  raw 999 -> theta1  25 deg
  raw 25  -> theta1 -180 deg

  请确认：

  - 这两个端点是你实测得到的吗？
  - raw 增大时，左腿 id0 是否确实从 25 deg 走向 -180 deg？——是的。
  - raw 增大时，右腿 id3 是否确实从 -180 deg 走向 25 deg？——是的。
  - 舵机反馈 g_ft_servos[idx].pos 是否能稳定读到当前位置？——是的。

  ## 5. 调试姿态

  为了避免一开始多个关节互相影响，建议先固定另一侧执行器。

  请确认左腿 DM4310 调试时，飞特舵机固定在哪个 raw：

  左腿飞特固定 raw = 500

  ## 6. 测量方式

  我们要调“精准”，需要知道你怎么判断准不准：

  - 你能直接读调试器变量吗？——你之间读，如果读不到你就调整反馈数据的回路，实在读不到，那就告诉我，我去检测硬件问题
      - g_dm_motors[].pos
      - g_ft_servos[].pos
      - g_app_obs.leg[].theta1_deg
      - g_app_obs.leg[].theta2_deg
      - g_app_obs.leg[].foot_d_x_mm/y_mm

  - 你是否能用尺子测足端 D 点坐标？——做不到
  - 你是否能用角度尺/标尺确认关节角？——做不到
  - 你更希望先以“电机反馈位置准确”为准，还是以“机构实际角度准确”为准？以电机反馈的位置为准，毕竟我也测试不了，咱有正解的相关代码的，你进行对比验证。

  ## 7. 控制参数初值

  当前 main.c 里 DM4310 用：

  kp = 6.0f
  kd = 0.15f

  请确认：

  - 这个刚度上电是否会太硬？有点软实际上，你可以根据反馈值与目标值的差别来进行调整，如果差别太大了，你就把 kp 降低一些，或者 kd 提高一些，反之则提高 kp 或降低 kd。


  ## 8. 你希望我先做的第一个调试程序

  我建议第一个程序非常简单：

  只启用左腿 DM4310
  飞特不动
  DM4310 在 3 个固定 rad 位置之间慢速循环
  调试器里观察反馈 pos 是否能稳定到目标

  请确认是否按这个开始。

  如果确认，请给我：

  - 左腿飞特固定 raw=500
  - 左腿 DM 三个目标 dm_rad=0.30, 1.10, 1.90
  - 允许位置误差=±0.05 rad
  - 是否使用当前 kp/kd，还是先降低，自己根据反馈值与目标值的差别来进行调整