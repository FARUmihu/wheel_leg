# 并联腿运动学待确认问题

## 当前接口量定义

新增接口只定义量，不参与当前运行路径。

| 量 | 方向/单位 | 说明 |
|---|---|---|
| `side` | `LEG_SIDE_LEFT` / `LEG_SIDE_RIGHT` | 左右按机器人自身视角定义 |
| `ft_raw` | 飞特原始位置计数 | 舵机原生 raw 值，暂不在接口层强行换算角度 |
| `fixed_ft_raw` | 飞特固定位置计数 | 飞特暂不参与调试时，用作固定几何输入 |
| `dm_rad` | rad | DM4310 电机反馈/目标位置，暂按电机轴位置记录 |
| `leg_length` | m | 腿变长为正 |
| `leg_angle` | rad | 腿相对机身向前摆为正 |

当前保留两套接口：

1. `leg_kinematics_full_forward()` / `leg_kinematics_full_inverse()`
   - 飞特和 DM4310 都作为关节自由度。
   - 正解输入 `ft_raw + dm_rad`，输出 `leg_length + leg_angle`。
   - 反解输入 `leg_length + leg_angle`，输出 `ft_raw + dm_rad`。

2. `leg_kinematics_fixed_ft_forward()`
   - 飞特作为固定几何输入，当前只把 DM4310 作为变量。
   - 输入 `fixed_ft_raw + dm_rad`，输出 `leg_length + leg_angle`。
   - 暂不提供固定飞特反解，因为固定飞特后只剩 1 个自由度，不能任意同时满足 `leg_length` 和 `leg_angle`。

现在这些函数都会返回 `LEG_KINEMATICS_NOT_IMPLEMENTED`，不会给出假结果。

## 需要你确认的数据

1. `leg_length` 最终用什么单位？建议用 m，方便以后和速度、力、IMU 数据统一。
答：m
2. `leg_angle` 最终用什么单位？建议用 rad，和 DM 电机位置一致。
答：rad
3. 左腿参考零位是什么姿态？请描述一个可重复摆出来的机械姿态。
答：左腿参考零位时，左腿处于收缩状态，DM4310电机的旋转中心与轮毂旋转中心的连线垂直于地面，但飞特舵机和DM4310电机轴不在同一平面，且都不知道具体角度，无法描述具体姿态，后续我给你提供测量数据。
4. 右腿参考零位是否和左腿镜像一致？如果不一致，需要单独描述。
答：右腿参考零位和左腿镜像一致。
5. 左腿在参考零位时，飞特 raw 值是多少？
答：左腿在参考零位时，飞特 raw 的值未知，后续我给你提供测量数据。
6. 左腿在参考零位时，DM4310 反馈 `pos` 是多少 rad？
答：左腿在参考零位时，DM4310 反馈 `pos` 的值未知，后续我给你提供测量数据。
7. 右腿在参考零位时，飞特 raw 值是多少？
答：右腿在参考零位时，飞特 raw 的值未知，后续我给你提供测量数据。
8. 右腿在参考零位时，DM4310 反馈 `pos` 是多少 rad？
答：右腿在参考零位时，DM4310 反馈 `pos` 的值未知，后续我给你提供测量数据。
9. 装配状态下，左/右飞特允许的 raw 安全范围分别是多少？
答：装配状态下，左/右飞特允许的 raw 安全范围未知，后续我给你提供测量数据。
10. 装配状态下，左/右 DM4310 允许的位置安全范围分别是多少 rad？
答：装配状态下，左/右 DM4310 允许的位置安全范围未知，后续我给你提供测量数据。
11. 并联腿的几何尺寸需要给出哪些点？建议至少提供侧视图中的机身铰点、飞特臂长、DM 连杆长度、末端轮轴点坐标或尺寸。
答：并联腿的几何尺寸参考文件"腿侧视图.jpg"，如有不理解，提问我进行补充
12. 左右腿是否完全镜像？如果有机械差异，后续需要两套参数。
答：左右腿完全镜像。

## 还需要补充的几何信息

`腿侧视图.jpg` 能看到尺寸，但点位还需要命名，否则公式容易写错。请后续补充：

1. 图中哪个点是飞特舵机输出轴中心？
2. 图中哪个点是 DM4310 输出轴中心？
3. 图中哪个点是轮毂旋转中心？
4. 图中哪些点是中间连杆铰点？建议用 `A/B/C/D/E` 标出来。
5. `leg_length` 应该定义为哪两个点之间的距离？建议确认是否为机身参考点到轮毂中心。
6. `leg_angle` 应该定义为哪条线相对机身的角度？建议确认是否为机身参考点到轮毂中心连线相对竖直或机身坐标的角度。
7. 飞特固定时，固定的是飞特 raw 值，还是固定某根连杆在图中的实际角度？
8. DM4310 的 `dm_rad = 0` 对应图中的哪根杆、哪个方向？

## 下一次实机测试建议

先不调试飞特舵机。下一次建议只做“固定飞特 + DM-only 数据采集”，目的是给固定飞特正解提供样本。

建议流程：

1. 机械上确认飞特在测试过程中保持固定，不写飞特位置，也不读写飞特作为调试目标。
2. 烧录当前固件，保持默认 `APP_MODE_IDLE`，确认 `g_app_status.control_ticks` 和 `background_ticks` 增长。
3. 通过 GDB 观察 DM4310 反馈 `g_app_obs.dm[APP_DM_LEFT_JOINT].pos` 或对应数组下标，先只记录反馈，不发运动命令。
4. 手动改变腿部姿态或使用极低风险方式改变 DM4310 位置，每个姿态记录：
   - `side`
   - `fixed_ft_raw` 或固定飞特机械姿态说明
   - `dm_rad`
   - 可测得的 `leg_length`
   - 可测得的 `leg_angle`
5. 左腿记录够以后，再按镜像关系处理右腿；右腿只需要少量样本验证镜像方向是否符合预期。

这一步不同时驱动整条腿，也不把飞特作为可调试模块。
