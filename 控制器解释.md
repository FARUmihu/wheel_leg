# 控制器解释

这份文档用于把“ESP32 指令解析”和“平衡状态下轮腿运动控制”补齐到可一次性实现的程度。请尽量在每个 `回答：` 后直接填写；如果你不确定，可以写“按建议默认”。

## 1. 当前工程事实

- USART1 当前为 `PB6 = TX`、`PB7 = RX`，`115200 8N1`。
- USART1 接收方式是 `HAL_UARTEx_ReceiveToIdle_DMA()`，收到一段数据后按字节喂给协议状态机。
- 当前协议帧格式：

```text
AA 55 cmd seq len payload crc_lo crc_hi
```

- CRC 当前是 Modbus CRC16，计算范围为 `cmd seq len payload`，不包含帧头。
- 当前只接受 `cmd = 0x01`、`len = 4`。
- 当前 payload 解释为两个 little-endian `int16_t`：

```text
payload[0..1] = vx_mm_s
payload[2..3] = wz_mrad_s
```

- 解析成功后立即更新 `g_remote_esp32`。
- 主循环调用 `app_remote_background()` 后，会把 `g_remote_esp32` 同步到 `g_app_remote_cmd`。
- 当前 `g_app_remote_cmd` 还没有接入 `app_run_balance()`，所以遥控量不会让车运动。
- 当前 `APP_TUNE_AUTOSTART = 1`，上电后会尝试自动进入初始腿姿态和平衡测试流程。

## 2. 指令协议需要确认

### 2.1 串口参数

最终 ESP32 与 STM32 使用的串口参数是什么？

回答：115200 8N1

### 2.2 ESP32 发送频率

ESP32 会以多少频率持续发送控制帧？是只在摇杆变化时发送，还是固定周期发送？

回答：20Hz。

### 2.3 当前 `cmd = 0x01` 是否就是运动指令

`cmd = 0x01` 是否只表示平衡状态下的速度控制？

回答：详细的指令解析你看文件“protocol.md”

### 2.4 是否需要更多命令

是否需要从 ESP32 发送模式切换或安全指令？

回答：暂时不需要。

### 2.5 `seq` 字段用途

`seq` 是否只用于调试丢包，还是需要 STM32 检查递增连续性？

回答：只记录，不因为跳号丢弃帧。

### 2.6 遥控死区

ESP32 是否已经做了摇杆死区？STM32 是否还需要做二次死区？

回答：不做。

## 3. 运动量含义需要确认

### 3.1 `vx_mm_s`

`vx_mm_s > 0` 在车上的物理含义是什么？

回答：车体向前运动。

### 3.2 `wz_mrad_s`

`wz_mrad_s > 0` 在车上的物理含义是什么？

回答：车体左转。

### 3.3 最大速度

第一版允许的最大前进/后退速度是多少？

回答：`300 mm/s`。

### 3.4 最大转向角速度

第一版允许的最大转向角速度是多少？

回答：`800 mrad/s`。

### 3.5 指令斜坡

速度指令是否需要斜坡限制，避免摇杆突变导致车猛冲？

回答：需要。`vx` 斜坡先用 `600 mm/s^2`，`wz` 斜坡先用 `1600 mrad/s^2`。

## 4. 平衡控制接入方式需要确认

当前平衡控制器输入是：你参考传统的平衡车的逻辑来设计。

```text
target_pitch_deg
pitch_deg
pitch_rate_rad_s
wheel_forward_vel_rad_s
dt_s
```

当前 `target_pitch_deg` 固定为 `0.0f`，所以车只会原地自平衡。

### 4.1 前进后退采用哪种方式

`vx_mm_s` 接入平衡控制时，优先采用哪种策略？

回答：选择策略A。

建议默认：把 `vx_mm_s` 转成目标俯仰角偏置 `target_pitch_deg`，先做小范围前进/后退。这样改动最小。

可选策略：

- A. 目标俯仰角偏置：`vx -> target_pitch_deg`
- B. 目标轮速闭环：`vx -> wheel_velocity_target`
- C. 直接扭矩前馈：`vx -> torque_bias`

### 4.2 `vx` 到俯仰角的比例

如果采用目标俯仰角偏置，`vx_mm_s` 应映射到多少最大目标俯仰角？

回答：`vx = 300 mm/s` 对应 `target_pitch = 3 deg`，并限制 `target_pitch` 在 `[-3, 3] deg`。

### 4.3 前进方向的俯仰符号

车向前时，目标俯仰角应该是正还是负？

回答：正。

### 4.4 转向采用哪种方式

`wz_mrad_s` 接入轮子时，优先采用哪种策略？

回答：参考传统平衡车的逻辑，建议采用策略A。

建议默认：在平衡输出扭矩基础上叠加左右轮差速扭矩 `turn_torque`。

可选策略：

- A. 左右轮差速扭矩：`left = balance + turn`，`right = balance - turn`
- B. 左右轮目标速度差：需要轮速闭环
- C. 暂不接转向，第一版只做前进后退

### 4.5 转向力度

如果采用差速扭矩，`wz_mrad_s` 到 `turn_torque` 的比例和最大值是多少？

回答：先把 `wz = 800 mrad/s` 映射到 `turn_torque = 0.25 N*m`，最大限制 `0.25 N*m`。

## 5. 轮子与电机方向需要确认

当前平衡发轮子扭矩的代码逻辑是：

```c
left_wheel.t = t;
right_wheel.t = -t;
```

当前前进速度估计是：

```c
wheel_forward_vel = 0.5f * (left_vel_filtered - right_vel_filtered);
```

### 5.1 实测前进方向

当左轮发送正扭矩、右轮发送负扭矩时，车体实际趋势是向前还是向后？

回答：向前。

### 5.2 左右轮差速方向

如果给左轮和右轮叠加同向/反向转向扭矩，哪种方向对应左转？

回答：同向为左转，反向为右转。

### 5.3 轮半径和轮距

如果后续做目标轮速闭环，需要轮半径和左右轮距。请填写：

回答：轮半径360mm。左右轮距是187mm。

## 6. 轮腿联动需要确认

### 6.1 第一版是否调整腿姿态

平衡运动时，腿部是否保持当前初始姿态不动？

回答：第一版腿部保持初始姿态，只控制轮子运动

### 6.2 是否启用腿部辅助平衡

当前 `APP_TUNE_USE_LEG_ASSIST = 0`。是否需要在遥控运动时启用腿部辅助平衡？

回答：第一版不启用，先把轮子运动闭环调通。

### 6.3 腿部高度/姿态指令

ESP32 后续是否需要发送腿高、站立高度、蹲起等指令？

回答：当前不放入。

## 7. 模式与安全需要确认

### 7.1 遥控何时生效

遥控速度应在哪些模式下生效？

回答：只在 `APP_MODE_BALANCE_TEST` 和 `APP_MODE_BALANCE_LEG_ASSIST` 下生效。

### 7.2 遥控离线行为

遥控离线后应该怎么做？

回答：`vx/wz` 立即斜坡归零，不退出平衡；

### 7.3 急停行为

如果 ESP32 发送急停，STM32 应该做什么？

回答：第一版没有急停命令；

### 7.4 自启动是否保留

当前 `APP_TUNE_AUTOSTART = 1`。接入遥控后是否继续保留上电自启动平衡？

回答：依旧保留。

## 8. 建议第一版实现范围

如果你没有特别指定，我建议第一版只做以下内容：

1. 保留当前 `cmd = 0x01` 协议，不改 ESP32 帧格式。
2. `g_app_remote_cmd.online == 1` 时读取 `vx_mm_s` 和 `wz_mrad_s`。
3. 只在平衡模式下生效。
4. `vx` 经过死区、限幅、斜坡后映射成 `target_pitch_deg`。
5. `wz` 经过死区、限幅、斜坡后映射成左右轮差速扭矩。
6. 腿部保持初始姿态，不做腿部轨迹。
7. 遥控离线时指令归零，平衡继续保持。
8. 增加调试变量，方便看 `remote_vx_cmd`、`remote_wz_cmd`、`target_pitch_deg`、`turn_torque`。

请确认是否按这个第一版范围做：

回答：除了不设置死区外，我觉得都ok，你更多认真看我对上述的回答。

