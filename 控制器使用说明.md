# 控制器使用说明

本文说明当前固件从上电到用 ESP32 控制轮腿车运动的最小流程。

## 1. 当前控制能力

当前版本支持 ESP32 通过 USART1 发送 `vx_mm_s` 和 `wz_mrad_s`：

- `vx_mm_s > 0`：车体向前
- `vx_mm_s < 0`：车体向后
- `wz_mrad_s > 0`：车体左转
- `wz_mrad_s < 0`：车体右转

遥控量只在 `APP_MODE_BALANCE_TEST` 或 `APP_MODE_BALANCE_LEG_ASSIST` 下生效。当前配置保留上电自启动平衡流程，腿部保持初始姿态，第一版只控制轮子运动。

## 2. 上电前检查

1. STM32 已烧录当前工程。
2. ESP32 串口接线正确：

```text
ESP32 GPIO17 TX -> STM32 PB7 USART1_RX
ESP32 GPIO18 RX -> STM32 PB6 USART1_TX
ESP32 GND       -> STM32 GND
```

3. 串口参数为 `115200 8N1`。
4. CAN1/CAN2、IMU、DM 电机、飞特舵机供电和接线正常。
5. 车体放在安全支撑或空旷地面，确保上电自启动不会伤人或撞物。

## 3. 上电到进入平衡

1. 给控制板、驱动和 ESP32 上电。
2. 固件启动后前 `APP_COLD_START_HOLD_MS = 3000 ms` 会反复 disable 电机，作为冷启动保护。
3. 固件等待 IMU 在线并稳定至少 `APP_IMU_ACTIVE_STABLE_MS = 1000 ms`。
4. 保持车体基本水平和静止：

```text
pitch 绝对值 <= APP_AUTOSTART_MAX_PITCH_DEG
roll  绝对值 <= APP_AUTOSTART_MAX_ROLL_DEG
gyro  绝对值 <= APP_AUTOSTART_MAX_GYRO_RAD_S
```

5. 满足条件后，固件执行初始腿姿态：

```text
left foot  = (-36, -80) mm
right foot = ( 36, -80) mm
```

6. 初始腿姿态完成后，再等待 `APP_TUNE_START_DELAY_MS = 8000 ms`，如果 IMU、姿态和关节在线条件仍满足，固件自动调用 `app_balance_start()` 进入 `APP_MODE_BALANCE_TEST`。
7. 进入平衡后，前 `APP_WHEEL_ENABLE_FRAMES` 帧只使能轮电机并发送零扭矩；之后遥控量开始参与控制。

## 4. 发送控制指令

ESP32 需要按 `20 Hz` 左右持续发送控制帧，不建议只发单帧。

帧格式：

```text
AA 55 CMD SEQ LEN PAYLOAD CRC_LO CRC_HI
```

当前唯一运动命令：

```text
CMD = 0x01
LEN = 0x04
payload[0..1] = int16 vx_mm_s, little-endian
payload[2..3] = int16 wz_mrad_s, little-endian
CRC = CRC-16/MODBUS over CMD SEQ LEN PAYLOAD
```

示例：

```text
前进 160 mm/s, SEQ=01: AA 55 01 01 04 A0 00 00 00 D9 D1
左转 500 mrad/s, SEQ=03: AA 55 01 03 04 00 00 F4 01 7D 33
停止, SEQ=05:          AA 55 01 05 04 00 00 00 00 FA 55
```

## 5. 控制映射

STM32 侧对遥控量做限幅和斜坡：

```text
vx 限幅: [-300, 300] mm/s
wz 限幅: [-800, 800] mrad/s
vx 斜坡: 600 mm/s^2
wz 斜坡: 1600 mrad/s^2
```

前进/后退采用目标俯仰角偏置：

```text
vx = 300 mm/s -> target_pitch = +3 deg
vx = -300 mm/s -> target_pitch = -3 deg
```

转向采用左右轮同向差速扭矩：

```text
wz = 800 mrad/s -> turn_torque = +0.25 N*m
wz = -800 mrad/s -> turn_torque = -0.25 N*m
```

最终轮电机命令为：

```text
left_wheel.t  = balance_torque + turn_torque
right_wheel.t = -balance_torque + turn_torque
```

其中左轮正扭矩、右轮负扭矩对应车体向前；左右轮叠加同向扭矩对应左转。

## 6. 判断是否成功

调试器中建议观察：

```text
g_remote_esp32.rx_frame_count
g_remote_esp32.crc_error_count
g_app_remote_cmd.online
g_app_remote_cmd.vx_mm_s
g_app_remote_cmd.wz_mrad_s
g_remote_dbg_vx_cmd_mm_s
g_remote_dbg_wz_cmd_mrad_s
g_remote_dbg_target_pitch_deg
g_remote_dbg_turn_torque
g_app_cmd.mode
```

成功链路应表现为：

1. ESP32 持续发帧后，`g_remote_esp32.rx_frame_count` 增加。
2. `g_app_remote_cmd.online = 1`。
3. 进入平衡模式后，`g_remote_dbg_target_pitch_deg` 随 `vx` 变化。
4. `g_remote_dbg_turn_torque` 随 `wz` 变化。
5. 车体在平衡状态下能前进、后退、左转、右转。

## 7. 停止与失联行为

- 正常停止：ESP32 持续发送 `vx=0, wz=0`。
- 遥控失联：超过 `APP_REMOTE_TIMEOUT_MS = 500 ms` 没收到合法帧后，STM32 将目标 `vx/wz` 置零，控制量按斜坡归零，不主动退出平衡。
- 当前第一版没有 ESP32 急停命令。需要急停时，应先停止发送运动指令，并通过现有调试入口或后续新增命令调用 `app_balance_stop()`。

## 8. 常见问题

如果 `g_remote_esp32` 有值但车不动，先确认 `g_app_cmd.mode` 是否已经进入 `APP_MODE_BALANCE_TEST` 或 `APP_MODE_BALANCE_LEG_ASSIST`。

如果 `g_app_remote_cmd` 没值，确认主循环没有停在断点，且 `app_remote_background()` 正在运行。

如果串口助手没有回显，这是正常现象；当前固件不发送 ACK。

如果方向相反，优先调整以下宏：

```c
APP_REMOTE_TARGET_PITCH_SIGN
APP_REMOTE_TURN_TORQUE_SIGN
APP_BALANCE_TORQUE_SIGN
APP_BALANCE_WHEEL_STATE_SIGN
```
