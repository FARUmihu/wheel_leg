# wheel_leg 后续开发步骤

## 当前判断

- 这是 STM32CubeIDE 工程：`Core/`、`Drivers/` 主要由 CubeMX 管，业务代码放 `user/`。
- 已有基础：CAN BSP、达妙电机、DM IMU、飞特舵机、PID、Kalman。
- 目标对象是双足轮腿机器人：每条腿 1 个飞特舵机关节、1 个 DM4310 关节、1 个 DM3510 轮毂；全车共 6 个执行器。
- 执行器 ID、总线和方向约定见 `mapping.md`。
- 主要缺口：`user/app`、`user/controller` 还没落地，`main.c` 仍是单电机测试循环，TIM6 2 kHz 控制入口还没真正接入。
- 先处理一个维护问题：部分源码注释存在编码显示异常，后续统一为 UTF-8，别顺手大改逻辑。

## 开发顺序

1. 固化工程基线  
   确认 CubeMX 配置、Debug 产物、include path 和当前外设配置。  
   验收：`make -C Debug -j` 能稳定通过，烧录后能进入 `main`。

2. 分执行器硬件自检  
   分别验证 CAN1 IMU、CAN2 上的 2 个 DM4310 + 2 个 DM3510、USART1 上的 2 个飞特舵机。  
   验收：各总线有反馈计数、无 bus-off、无异常超时。

3. 建 `user/app` 调度层  
   增加 `app_init()`、`app_control_2khz()`、`app_background()`；`main.c` 只负责初始化和后台循环，TIM6 回调只调用实时步进函数。  
   验收：GPIO 或计数器证明 2 kHz 周期稳定。

4. 统一设备状态接口  
   每个设备提供物理量、在线状态、超时、错误码；禁止在 2 kHz ISR 里做阻塞 UART 读取。  
   验收：拔线/掉帧能被检测，电机输出能安全归零。

5. 建 `user/controller` 控制层  
   先按单腿拆：飞特关节位置、DM4310 关节阻抗/位置、DM3510 轮速/力矩；再组合成腿长、腿角、轮速控制。  
   验收：每个控制器都有输出限幅，能单独低增益测试。

6. 整车状态机和算法接入  
   `algorithm/` 只放纯数学，模式切换、使能逻辑、故障处理放 `app/`。  
   验收：站立、失能、故障、复位这些状态路径可重复验证。

7. 安全和调参  
   补急停、CAN 超时、温度/错误位保护、参数表和必要遥测。  
   验收：任一关键故障都会进入零力矩或失能状态。

8. 小步闭环迭代  
   每次只改一个硬件链路或一个控制环：构建、烧录、记录、提交。  
   验收：问题能回滚到最近一次可用状态。

## 近期第一步

先不要直接写平衡控制。建议下一步只做 `user/app` 的最小调度骨架，把现在 `main.c` 里的单电机测试迁出去，并把 TIM6 2 kHz 入口跑通。
